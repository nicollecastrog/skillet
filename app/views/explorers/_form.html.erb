<%= form_for(@explorer) do |f| %>
  <% if @explorer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@explorer.errors.count, "error") %> prohibited this explorer from being saved:</h2>

      <ul>
      <% @explorer.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :gender %><br />
    <%= f.text_field :gender %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
